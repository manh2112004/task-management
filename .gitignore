# 📦 Thư mục chứa thư viện cài qua npm
node_modules/

# ⚙️ File log (lỗi, ghi chép...)
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 🧪 Kết quả kiểm thử
coverage/

# 📂 Thư mục build hoặc dist (khi transpile từ TS/Babel/Webpack...)
dist/
build/

# ⚙️ File cấu hình môi trường (nhạy cảm!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 💻 Hệ điều hành sinh ra
.DS_Store
Thumbs.db

# 💼 VSCode và các editor khác
.vscode/
.idea/

# 🔄 Tệp tạm của hệ thống hoặc khi merge conflict
*.swp
*.swo
*.tmp
*.bak

# 🔧 Nếu dùng TypeScript
*.tsbuildinfo

# 🛠️ Nếu dùng PM2 để quản lý process
pids/
*.pid

# 📜 Nếu dùng MongoDB local dump
dump/

# 📦 Lock file nếu dùng Yarn (nếu chỉ dùng npm)
yarn.lock

# ❗ Nếu dùng Docker
docker-compose.override.yml
